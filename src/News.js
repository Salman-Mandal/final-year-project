import React from 'react'
import { View, Text } from 'react-native';
const News = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
          <Text style={{ fontSize: 40, color: 'green' }}>News Screen</Text>
        </View>
      );
}

export default News

